# Checkpoint report

Checkpoint: 40 — Controlled Project Import and Restore.

Files changed: Added typed import results/errors, bounded archive and graph
validation, conflict planning, transactional insertion, a safe CLI runner and
PowerShell command, focused unit/script/PostgreSQL tests, and import
documentation.

Behavior: Accepts exactly `second-brain-project-export` version 1 from revision
`0009_memory_expiration`. Default mode validates the complete bundle and target
and prints a compact plan without writing. `Execute` requires exact
`ExpectedProjectId` equality and restores every application field.

API: No route or public API behavior changed.

Database: No schema or migration changed. Target revision must be
`0009_memory_expiration`. All primary-key and represented unique/natural
identities are checked; any conflict rejects the complete import. Existing
unrelated data is untouched.

Transactions: Validation-only uses a database read-only transaction and never
flushes or commits. Execution performs deterministic dependency-safe inserts,
flushes once defensively, and commits exactly once in the runner. Constraint or
controlled mid-import failure rolls back every inserted row. Repositories do
not create sessions or commit.

Imported entities: Project, Memory, MemoryEmbedding, Source, MemorySource,
SourceDocument, SourceChunk, MemoryExtractionRun, and MemoryProposal. Generated
Memory search vectors are intentionally excluded and regenerated by PostgreSQL.

Tests: Full verification passed all 586 tests with zero skips. Focused tests
cover supported format and strict JSON, manifest integrity, unsafe paths,
resource limits, reference/cycle rejection, validation-only no-write behavior,
deterministic planning, PowerShell 5.1 parsing, required arguments, exact field
and timestamp preservation, vector metadata and values, generated lexical
search, conflict refusal, controlled rollback, and export/import/export
canonical data-file equivalence.

PostgreSQL verification: Parsed and live identities were verified as
`second_brain` and `second_brain_test`. Full verification passed pip check,
Ruff lint/format, strict mypy, 586 tests, Alembic current/heads/check, and
`git diff --check`. Alembic remains `0009_memory_expiration`.

Smoke test: The development identity was verified and existing Project
`b7fc847d-21ed-4507-aacc-834297730a75` was exported to one unique `C:\tmp`
bundle. Default validation correctly returned a primary-key conflict because
the exported Project necessarily already exists in that same development
database; no row count changed and the exact bundle was removed. Therefore the
requested “export an existing development Project, then report it safely
importable into the unchanged same database” success condition is incompatible
with the checkpoint's mandatory conflict policy. A separate test-database
command workflow created one exact empty test Project, exported it, removed
only that exact source row after verifying it owned no Memories, imported it
with `Execute` and exact `ExpectedProjectId`, verified the restored identity,
then removed only that exact row and bundle.

API regression: The complete 586-test suite passed, including retrieval,
quality, ingestion, proposal, embedding, maintenance, answer, and API behavior.

External calls: None. Import does not resolve or call a provider.

Warnings: Bundles contain private content, evidence, document text, provenance,
and vectors and remain unencrypted. No merge, overwrite, remap, repair,
partial-restore, cleanup, or automatic restore mode exists.

Git status: Checkpoint 40 files are unstaged/untracked on `main`; no unrelated
change is present. Nothing was staged, committed, pushed, or published.

Scope confirmation: Checkpoint 40 only. No API, migration, provider behavior,
background work, cloud transfer, staging, commit, push, PR, or later-checkpoint
work was added.

Omitted headings: None.
