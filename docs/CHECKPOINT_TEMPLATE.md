# Checkpoint [number]: [title]

Before acting, read `AGENTS.md`, `docs/ARCHITECTURE.md`, `docs/ROADMAP.md`,
`docs/CHECKPOINTS.md`, `docs/VERIFICATION.md`, `docs/SAFETY.md`,
`docs/API_CONVENTIONS.md`, and relevant ADRs in `docs/decisions/`.

Goal:

Preconditions:

Allowed scope:

Forbidden scope:

API contract:

Database changes:

Transaction rules:

Acceptance tests:

PostgreSQL integration tests:

Smoke scenario:

Documentation changes:

Report format: use `docs/REPORT_TEMPLATE.md`.

Do not commit or push.
